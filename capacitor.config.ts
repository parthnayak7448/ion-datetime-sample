import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ion Date Sample App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
